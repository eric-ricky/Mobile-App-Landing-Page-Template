/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['lh3.googleusercontent.com', 'ke.jumia.is', 'www.instacart.com'],
  },
};

module.exports = nextConfig;
